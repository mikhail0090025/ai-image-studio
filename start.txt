docker-compose down
docker-compose build
docker-compose up -d

Tests:
cd frontend
docker exec -it ai_image_studio_frontend python -m pytest
cd ..

Kill Docker if stucks:
wsl --shutdown
taskkill /F /IM "Docker Desktop.exe"
taskkill /F /IM "com.docker.backend.exe"

Full dependencies list:
docker exec -it ai_image_studio_ai_service pip freeze > ./ai_service/requirements.full.txt
docker exec -it ai_image_studio_frontend pip freeze > ./frontend/requirements.full.txt

Reload frontend ONLY:
docker-compose down frontend
docker-compose build frontend
docker-compose up -d frontend